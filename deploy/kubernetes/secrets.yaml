# TEMPLATE ONLY -- do not commit real secrets. Populate via a sealed-secrets
# controller, External Secrets Operator, or `kubectl create secret` in CI/CD.
apiVersion: v1
kind: Secret
metadata:
  name: ocr-secrets
  namespace: ocr-service
type: Opaque
stringData:
  OCR_DATABASE_URL: "postgresql+asyncpg://ocr_app:REPLACE_ME@postgres:5432/ocr"
  OCR_MIGRATION_DATABASE_URL: "postgresql+psycopg://postgres:REPLACE_ME@postgres:5432/ocr"
  OCR_QDRANT_API_KEY: "REPLACE_ME"
  OCR_PII_ENCRYPTION_KEY: "REPLACE_ME_BASE64_32_BYTES"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  LLAMA_CLOUD_API_KEY: "REPLACE_ME"
